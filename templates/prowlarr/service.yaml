apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.prowlarr.appName }}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 9696
  selector:
    app: {{ .Values.prowlarr.appName }}
  type: ClusterIP
