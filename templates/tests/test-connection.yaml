apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "home-server-helm.fullname" . }}-test-connection"
  labels:
    {{- include "home-server-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "home-server-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
