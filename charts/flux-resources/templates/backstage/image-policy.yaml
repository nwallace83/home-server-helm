apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: backstage
  namespace: backstage
spec:
  imageRepositoryRef:
    name: backstage
  filterTags:
    pattern: '^\d{14}$'
  policy:
    alphabetical:
      order: asc