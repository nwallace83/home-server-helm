apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: backstage
  namespace: backstage
spec:
  imageRepositoryRef:
    name: backstage
  policy:
    alphabetical:
      order: asc