apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: prowlarr
  namespace: home-server
spec:
  imageRepositoryRef:
    name: prowlarr
  policy:
    semver:
      range: ">=1.25.4"