apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: wireguard
  namespace: home-server
spec:
  imageRepositoryRef:
    name: wireguard
  policy:
    semver:
      range: ">=1.0.0"