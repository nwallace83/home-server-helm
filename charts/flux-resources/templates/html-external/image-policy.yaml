apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: nginx
  namespace: home-server
spec:
  imageRepositoryRef:
    name: nginx
  policy:
    semver:
      range: ">=1.27.2"