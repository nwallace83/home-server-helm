apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: vaultwarden
  namespace: home-server
spec:
  image: harbor.nwallace.io/docker-hub/vaultwarden/server
  interval: 30m