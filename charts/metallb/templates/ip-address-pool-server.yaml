apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: server-address
  labels:
    {{- include "commonLabels" . | nindent 4 }}
    {{- .Values.commonLabels | nindent 4 }}
spec:
  addresses:
  - 192.168.0.5/32
  - 192.168.0.15/32
  autoAssign: false
  avoidBuggyIPs: false