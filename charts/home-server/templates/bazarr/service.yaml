{{- $appName := "bazarr" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $appName }}
spec:
  externalTrafficPolicy: Local
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 6767
  selector:
    app: {{ $appName }}
