apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.pihole.appName }}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: {{ .Values.pihole.appName }}