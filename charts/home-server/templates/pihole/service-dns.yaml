apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.pihole.appName }}-dns
spec:
  ports:
    - name: dns-tcp
      protocol: TCP
      port: 53
      targetPort: 53
    - name: dns-udp
      protocol: UDP
      port: 53
      targetPort: 53
  selector:
    app: {{ .Values.pihole.appName }}
