apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.handbrake.appName }}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5800
  selector:
    app: {{ .Values.handbrake.appName }}
  type: ClusterIP
