{{- with .Values.applications.handbrake }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .appName }}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5800
  selector:
    app: {{ .appName }}
  type: ClusterIP
{{- end }}