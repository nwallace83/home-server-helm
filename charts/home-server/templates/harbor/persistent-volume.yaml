apiVersion: v1
kind: PersistentVolume
metadata:
  name: harbor-pv
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: server.local
    path: /harbor
  mountOptions:
    - nfsvers=4.2
    - soft
    - timeo=600
    - retrans=2
    - noresvport
  persistentVolumeReclaimPolicy: Retain