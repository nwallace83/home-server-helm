apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vault-backup
  namespace: vault
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: vault-backup