{{- $appName := "vault-auto-unsealer" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $appName }}
  namespace: vault
  labels:
    app: {{ $appName }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ $appName }}
  template:
    metadata:
      labels:
        app: {{ $appName }}
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/arch
                operator: In
                values:
                - amd64
      containers:
        - name: {{ $appName }}
          image: blockloop/vault-unseal:latest
          env:
          - name: VAULT_ADDR
            value: http://vault-internal:8200
          - name: VAULT_UNSEAL_KEY_1 
            value: {{ .Values.vault.unsealKey }}
  strategy: 
    type: Recreate
  revisionHistoryLimit: 0