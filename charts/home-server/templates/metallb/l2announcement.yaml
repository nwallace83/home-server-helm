{{- if eq .Values.environment "production" }}
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: {{ .Values.metallb.ipPoolName }}
  namespace: metallb
spec:
  ipAddressPools:
  - {{ .Values.metallb.ipPoolName }}
{{- end }}