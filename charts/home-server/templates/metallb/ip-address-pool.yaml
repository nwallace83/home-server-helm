{{- with .Values.metallb }}
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: {{ .ipPoolName }}
  namespace: metallb
spec:
  addresses:
  {{- range .adresses }}
  - {{ . }}
  {{- end }}
  autoAssign: false
  avoidBuggyIPs: false
{{- end }}
