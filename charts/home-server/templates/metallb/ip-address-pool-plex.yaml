{{- if eq .Values.environment "production" }}
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: plex-address
  namespace: metallb
spec:
  addresses:
  - 192.168.0.4/32
  autoAssign: false
  avoidBuggyIPs: false
{{- end }}
