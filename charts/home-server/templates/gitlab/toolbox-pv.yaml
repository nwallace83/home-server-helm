apiVersion: v1
kind: PersistentVolume
metadata:
  name: gitlab-toolbox-pv
spec: 
  storageClassName: gitlab-toolbix
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /disk2/backup/gitlab
  persistentVolumeReclaimPolicy: Retain
