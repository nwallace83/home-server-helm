apiVersion: v1
kind: PersistentVolume
metadata:
  name: gitlab-toolbox-pv
spec: 
  storageClassName: gitlab-toolbox
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /kubernetes/gitlab/toolbox
  persistentVolumeReclaimPolicy: Retain
