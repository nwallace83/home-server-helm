apiVersion: v1
kind: PersistentVolume
metadata:
  name: gitlab-toolbox-backup-pv
spec: 
  storageClassName: gitlab-toolbox-backup
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /disk2/backup/gitlab
  persistentVolumeReclaimPolicy: Retain
