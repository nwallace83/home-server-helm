apiVersion: v1
kind: PersistentVolume
metadata:
  name: tv-shows
spec:
  capacity:
    storage: 2048Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: server.local
    path: "/TV_Shows"
  mountOptions:
    - nfsvers=4.2
  persistentVolumeReclaimPolicy: Retain
