apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tv-shows
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 2048Gi
  volumeName: tv-shows
