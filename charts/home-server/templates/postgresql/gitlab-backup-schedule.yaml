apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: gitlab-backup
  namespace: gitlab
spec:
  schedule: "0 0 0 * * *"
  backupOwnerReference: cluster
  immediate: true
  cluster:
    name: gitlab