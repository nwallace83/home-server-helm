{{- $appName := "pgpool" }}
apiVersion: v1
kind: Service
metadata:
  name: gitlab-{{ $appName }}
  namespace: gitlab
spec:
  ports:
    - name: http
      protocol: TCP
      port: 5432
      targetPort: 5432
  selector:
    app: {{ $appName }}