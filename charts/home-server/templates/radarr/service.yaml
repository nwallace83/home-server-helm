{{- $appName := "radarr" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $appName }}
spec:
  externalTrafficPolicy: Local
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 7878
  selector:
    app: {{ $appName }}