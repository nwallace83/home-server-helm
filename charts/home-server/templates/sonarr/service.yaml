{{- $appName := "sonarr" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $appName }}
spec:
  externalTrafficPolicy: Local
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8989
  selector:
    app: {{ $appName }}