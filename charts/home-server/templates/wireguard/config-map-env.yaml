{{- with .Values.applications.wireguard }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .appName }}-env
data:
  PGID: "{{ $.Values.localGID }}"
  PUID: "{{ $.Values.localUID }}"
  TZ: {{ $.Values.timezone }}
  SERVERURL: nwallace.io
  PEERS: "5"
  LOG_CONFS: "true"
  SERVERPORT: "51820"
  LOG_CONFS: "true"
{{- end }}