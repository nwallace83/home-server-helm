{{- with .Values.applications.sickchill }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .appName }}-env
data:
  PGID: "{{ $.Values.localGID }}"
  PUID: "{{ $.Values.localUID }}"
  TZ: {{ $.Values.timezone }}
{{- end }}