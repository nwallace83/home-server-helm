{{- with .Values.applications.sickchill }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .appName }}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8081
  selector:
    app: {{ .appName }}
  type: ClusterIP
{{- end }}