apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.sickchill.appName }}
  labels:
    app: {{ .Values.sickchill.appName }}
    test: deleteme
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.sickchill.appName }}
  template:
    metadata:
      labels:
        app: {{ .Values.sickchill.appName }}
    spec:
      {{- if .Values.sickchill.volumes  }}
      volumes:
      {{- range .Values.sickchill.volumes }}
        - name: {{ .name }}
          hostPath:
            path: {{ .host_path }}
      {{- end }}
      {{- end }}
      containers:
        - name: {{ .Values.sickchill.appName }}
          image: linuxserver/sickchill:latest
          ports:
            - containerPort: 8081
              protocol: TCP
          envFrom:
            - configMapRef:
                name: "{{ .Values.sickchill.appName }}-env"
                optional: false
          {{- if .Values.sickchill.volumes  }}
          volumeMounts:
          {{- range .Values.sickchill.volumes }}
            - name: {{ .name }}
              mountPath: {{ .container_path }}
          {{- end }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: /
              port: 8081
              scheme: HTTP
            initialDelaySeconds: 60
            timeoutSeconds: 1
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 8081
              scheme: HTTP
            initialDelaySeconds: 60
            timeoutSeconds: 1
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 3
          imagePullPolicy: Always
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  revisionHistoryLimit: 0
