{{- $appName := "homeassistant" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $appName }}-env
data:
  TZ: {{ .Values.timezone }}
  PGID: {{ .Values.localGID | quote }}
  PUID: {{ .Values.localUID | quote }}