apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.plex.appName }}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 32400
  selector:
    app: {{ .Values.plex.appName }}
  type: ClusterIP
