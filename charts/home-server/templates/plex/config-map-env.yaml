{{- with .Values.applications.plex }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .appName }}-env
data:
  ADVERTISE_IP: >-
    http://{{ .loadBalancerIP }}:32400,http://plex.{{ $.Values.domain }}:32400
  TZ: {{ $.Values.timezone }}
  PGID: "{{ $.Values.localGID }}"
  PUID: "{{ $.Values.localUID }}"
  VERSION: docker
{{- end }}