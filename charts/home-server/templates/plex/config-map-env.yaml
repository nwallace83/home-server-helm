{{- with .Values.applications.plex }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .appName }}-env
data:
  ADVERTISE_IP: >-
    http://{{ $.Values.localIP }}:32400,http://plex.{{ $.Values.domain }}:32400,http://plex.{{ $.Values.domain }}
  TZ: {{ $.Values.timezone }}
  PGID: "{{ $.Values.localGID }}"
  PUID: "{{ $.Values.localUID }}"
  VERSION: docker
{{- end }}