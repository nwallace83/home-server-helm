{{- with .Values.applications.plex }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .appName }}-tcp
spec:
  ports:
    - name: {{ .appName }}-tcp
      protocol: TCP
      port: 32400
      targetPort: 32400
  selector:
    app: {{ .appName }}
{{- end }}