{{- with .Values.applications.plex }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .appName }}-tcp
spec:
  ports:
    - name: {{ .appName }}-tcp
      protocol: TCP
      port: 32400
      targetPort: 32400
      nodePort: 32400
  type: NodePort
  selector:
    app: {{ .appName }}
{{- end }}