apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.prowlarr.appName }}
  labels:
    app: {{ .Values.prowlarr.appName }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.prowlarr.appName }}
  template:
    metadata:
      labels:
        app: {{ .Values.prowlarr.appName }}
    spec:
      {{- if .Values.prowlarr.volumes  }}
      volumes:
      {{- range .Values.prowlarr.volumes }}
        - name: {{ .name }}
          hostPath:
            path: {{ .host_path }}
      {{- end }}
      {{- end }}
      containers:
        - name: {{ .Values.prowlarr.appName }}
          image: linuxserver/prowlarr:latest
          ports:
            - containerPort: 9696
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ .Values.prowlarr.appName }}-env
                optional: false
          resources: {}
          {{- if .Values.prowlarr.volumes  }}
          volumeMounts:
          {{- range .Values.prowlarr.volumes }}
            - name: {{ .name }}
              mountPath: {{ .container_path }}
          {{- end }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: /
              port: 9696
              scheme: HTTP
            initialDelaySeconds: 60
            timeoutSeconds: 1
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 9696
              scheme: HTTP
            initialDelaySeconds: 60
            timeoutSeconds: 1
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  revisionHistoryLimit: 0
  progressDeadlineSeconds: 600
