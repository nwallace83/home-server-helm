apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.radarr.appName }}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 7878
  selector:
    app: {{ .Values.radarr.appName }}
  type: ClusterIP
