apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: flux-resources
  annotations:
    'backstage.io/kubernetes-id': flux-resources
    'backstage.io/kubernetes-cluster': home-server
spec:
  owner: user:nate
  lifecycle: production
  type: service
