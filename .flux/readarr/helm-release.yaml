apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: readarr
spec:
  interval: 1m
  chart:
    spec:
      chart: ./charts/readarr
      sourceRef:
        kind: GitRepository
        name: home-server
      interval: 1m
  driftDetection:
    mode: enabled