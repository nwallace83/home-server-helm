apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: delugevpn
spec:
  interval: 1m
  chart:
    spec:
      chart: ./charts/delugevpn
      sourceRef:
        kind: GitRepository
        name: home-server
      interval: 1m
  driftDetection:
    mode: enabled