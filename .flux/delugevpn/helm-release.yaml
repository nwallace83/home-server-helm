apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: delugevpn
spec:
  interval: 10m
  chart:
    spec:
      chart: ./charts/delugevpn
      sourceRef:
        kind: GitRepository
        name: home-server
      reconcileStrategy: Revision
  driftDetection:
    mode: enabled
