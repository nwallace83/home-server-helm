apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: radarr
spec:
  interval: 1m
  chart:
    spec:
      chart: ./charts/radarr
      sourceRef:
        kind: GitRepository
        name: home-server
      interval: 1m