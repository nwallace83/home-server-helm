apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: prowlarr
spec:
  interval: 1m
  chart:
    spec:
      chart: ./charts/prowlarr
      sourceRef:
        kind: GitRepository
        name: home-server
      interval: 1m
  driftDetection:
    mode: enabled