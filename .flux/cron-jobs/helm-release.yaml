apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cron-jobs
spec:
  interval: 1m
  chart:
    spec:
      chart: ./charts/cron-jobs
      sourceRef:
        kind: GitRepository
        name: home-server
      reconcileStrategy: Revision
  driftDetection:
    mode: enabled