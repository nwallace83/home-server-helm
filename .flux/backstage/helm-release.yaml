apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: backstage
spec:
  interval: 10m
  targetNamespace: backstage
  chart:
    spec:
      chart: backstage
      version: '*'
      sourceRef:
        kind: HelmRepository
        name: backstage
      interval: 10m
  values: