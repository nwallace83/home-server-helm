apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: backstage
  namespace: backstage
spec:
  interval: 10m
  chart:
    spec:
      chart: backstage
      version: '*'
      sourceRef:
        kind: HelmRepository
        name: backstage
        namespace: backstage
      interval: 10m
  values: