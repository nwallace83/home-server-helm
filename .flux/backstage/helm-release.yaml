apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: backstage
spec:
  interval: 1m
  targetNamespace: backstage
  chart:
    spec:
      chart: ./charts/backstage
      sourceRef:
        kind: GitRepository
        name: home-server
      interval: 1m