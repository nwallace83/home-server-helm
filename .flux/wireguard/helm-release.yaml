apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wireguard
spec:
  interval: 1m
  chart:
    spec:
      chart: ./charts/wireguard
      sourceRef:
        kind: GitRepository
        name: home-server
      interval: 1m
  driftDetection:
    mode: enabled